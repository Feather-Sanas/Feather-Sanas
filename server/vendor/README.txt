Drop your Sanas SDK tarball here, e.g.:
  sanas_remote_sdk_linux_x86-64_<version>.tar.gz

Download it from https://developer.sanas.ai (Getting Started > Quick Start).
The Docker build auto-detects and installs it. Without it, the backend runs in MOCK mode.
